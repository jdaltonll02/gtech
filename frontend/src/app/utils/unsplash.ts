// Placeholder for unsplash utility
// In a real implementation, this would use the unsplash_tool
export const unsplash_tool = async (query: string): Promise<string> => {
  // This is a placeholder - the actual implementation would use the unsplash_tool
  return `https://source.unsplash.com/600x800/?${encodeURIComponent(query)}`;
};
